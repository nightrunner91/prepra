---
interface FAQItem {
  question: string;
  answer: string;
}

interface Props {
  items: FAQItem[];
}

const { items } = Astro.props;
---

<section class="mx-auto max-w-3xl">
  <h2
    class="font-serif text-3xl text-text sm:text-5xl leading-snug tracking-tight font-medium"
  >
    Вопросы и ответы
  </h2>
  <p class="mt-2 text-lg text-text-secondary leading-relaxed">
    Всё что нужно знать о prepra.
  </p>

  <div
    class="mt-8 divide-y divide-border border-t border-b border-border"
    data-faq
  >
    {
      items.map((item, i) => (
        <div class="faq-item" data-faq-item>
          <button
            type="button"
            class="faq-trigger flex w-full cursor-pointer items-center justify-between py-4 text-left font-serif text-lg text-text transition-colors hover:text-accent"
            aria-expanded="false"
            aria-controls={`faq-panel-${i}`}
            id={`faq-btn-${i}`}
          >
            <span>{item.question}</span>
            <svg
              class="faq-chevron ml-4 shrink-0 size-5 text-text-tertiary transition-transform duration-300"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="m19.5 8.25-7.5 7.5-7.5-7.5"
              />
            </svg>
          </button>
          <div
            class="faq-panel grid transition-[grid-template-rows] duration-300 ease-out"
            style="grid-template-rows: 0fr"
            id={`faq-panel-${i}`}
            role="region"
            aria-labelledby={`faq-btn-${i}`}
          >
            <div class="overflow-hidden">
              <div class="pb-4 text text-text-secondary leading-relaxed">
                <Fragment set:html={item.answer} />
              </div>
            </div>
          </div>
        </div>
      ))
    }
  </div>
</section>

<style>
  .faq-trigger[aria-expanded="true"] .faq-chevron {
    transform: rotate(180deg);
  }
</style>

<script>
  document.querySelectorAll<HTMLElement>("[data-faq]").forEach((faq) => {
    const items = faq.querySelectorAll<HTMLElement>("[data-faq-item]");

    items.forEach((item) => {
      const trigger = item.querySelector<HTMLButtonElement>(".faq-trigger")!;
      const panel = item.querySelector<HTMLDivElement>(".faq-panel")!;

      trigger.addEventListener("click", () => {
        const isOpen = trigger.getAttribute("aria-expanded") === "true";

        items.forEach((other) => {
          const otherTrigger =
            other.querySelector<HTMLButtonElement>(".faq-trigger")!;
          const otherPanel = other.querySelector<HTMLDivElement>(".faq-panel")!;
          if (otherTrigger !== trigger) {
            otherTrigger.setAttribute("aria-expanded", "false");
            otherPanel.style.gridTemplateRows = "0fr";
          }
        });

        trigger.setAttribute("aria-expanded", String(!isOpen));
        panel.style.gridTemplateRows = isOpen ? "0fr" : "1fr";
      });
    });
  });
</script>
